name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test & Lint
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: ['1.22', '1.23']

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go }}

    - name: Verify Dependencies
      run: go mod verify

    - name: Static Analysis (go vet)
      run: go vet ./...

    - name: Run Test Suite
      run: go test ./... -v

    - name: Build Binary
      run: go build -v ./cmd/porta
